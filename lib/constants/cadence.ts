/** Max single-image Poster rows per month on a client profile or generator poster override. */
export const MAX_POSTS_PER_MONTH = 30;

/** Max multi-slide Carousel rows per month on a client profile or generator override. */
export const MAX_CAROUSELS_PER_MONTH = 10;

/** Max Animated (motion) rows per month on a client profile or generator override. */
export const MAX_ANIMATED_PER_MONTH = 10;

/** Server ceiling for one month’s calendar (special days can raise counts toward this). */
export const MAX_CALENDAR_ROWS_PER_MONTH = 62;
